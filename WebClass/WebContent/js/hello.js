$(document).ready(function () {
    $.fn.snow({ minSize: 5, maxSize: 50, newOn: 1000, flakeColor: '#292929' });
          var val = location.href.substr(
              location.href.lastIndexOf('=') + 1
          );
      });